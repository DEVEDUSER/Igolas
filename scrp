local Rayfield
do
    local ok, result = pcall(function()
        local source = game:HttpGet("https://sirius.menu/rayfield", true)
        local fn = loadstring(source)
        return fn()
    end)
    if ok and result then
        Rayfield = result
    else
        warn("Falha ao carregar Rayfield:", result)
        return
    end
end

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")

local player = Players.LocalPlayer
local RE = ReplicatedStorage:WaitForChild("RE")
local RF = ReplicatedStorage:WaitForChild("RF")

local PlayerData = require(ReplicatedStorage.Modules.PlayerData)
local ItemData = require(ReplicatedStorage.Shared.ItemData)
local Clock = require(ReplicatedStorage.Shared.Clock)
local BoxController = require(ReplicatedStorage.Controllers.BoxController)
local StateUtil = require(ReplicatedStorage.Shared.StateUtil)

local I = {
    box = utf8.char(0x1F4E6),
    gift = utf8.char(0x1F381),
    event = utf8.char(0x1F389),
    money = utf8.char(0x1F4B0),
    shield = utf8.char(0x1F6E1),
    attack = utf8.char(0x2694),
    sell = utf8.char(0x1F4B8),
    map = utf8.char(0x1F5FA),
    wheel = utf8.char(0x1F3A1),
    quest = utf8.char(0x1F4DC),
    bell = utf8.char(0x1F514),
    stats = utf8.char(0x1F4CA),
    eye = utf8.char(0x1F441),
    gear = utf8.char(0x2699),
    star = utf8.char(0x2B50),
    egg = utf8.char(0x1F95A),
    hat = utf8.char(0x1F3A9),
    pet = utf8.char(0x1F43E),
    check = utf8.char(0x2705),
    x = utf8.char(0x274C),
    bomb = utf8.char(0x1F4A3),
}

local State = {
    autoGoldRush = false,
    autoEasterRush = false,
    autoClose = false,
    autoCloseRushResults = false,
    autoClaim = false,
    autoBP = false,
    autoDaily = false,
    autoAch = false,
    autoPerk = false,
    autoSellHats = false,
    autoSellPets = false,
    autoSellPerks = false,
    autoAttackBoxes = false,
    autoAttackCameraLock = false,
    autoWheel = false,
    autoQuestClaim = false,
    webhookEnabled = false,
    camLock = false,
    tpCamera = false,
}

local crateAmount = 1
local autoRushWorld = 108
local autoAttackRange = 65
local autoAttackDelay = 0.05
local autoSellDelay = 5

local webhookUrl = ""
local webhookDelayMinutes = 60
local lastWebhookAt = os.clock()

local autoFavoriteRareDrops = true
local autoFavoriteGoodPerks = true
local perkFavoriteMinStat = 4.0

local wheelDelay = 0.03
local wheelBurst = 1
local selectedWheelId = "Auto Detect"
local lastWheelTick = 0

local questClaimDelay = 0.5
local lastQuestClaimTick = 0

local merchantScanDelay = 0.25
local merchantLastScan = 0
local merchantSelectedItems = {}
local merchantBoughtCache = {}
local merchantAutoBuyEnabled = false

local camConn
local camAttackConn
local tpConn
local areaConn
local lockedCameraCenter

local routePoints = {}
local routeIndex = 1
local routeRadius = 80
local routeStep = 25

local autoFarmArea = false
local farmAreaIndex = 1
local farmAreaRadius = 90
local farmAreaStep = 28
local farmAreaCenter
local farmPoints = {}
local farmPointIndex = 1

local Stats = {
    startedAt = os.clock(),
    boxesBroken = 0,
    itemsSold = 0,
    hatsSold = 0,
    petsSold = 0,
    perksSold = 0,
    rushesDone = 0,
    rushesByName = {},
    cratesOpened = 0,
    cratesByName = {},
    eggRainGained = 0,
    bombRainGained = 0,
    ticketsGained = 0,
    merchantBought = 0,
}

local sellRarities = {
    Hats = { Common = true, Rare = true, Epic = false, Legendary = false, Godly = false, Mythic = false, Etherium = false, Celestial = false },
    Pets = { Common = true, Rare = true, Epic = false, Legendary = false, Godly = false, Mythic = false, Etherium = false, Celestial = false },
    Perks = { Common = true, Rare = true, Epic = false, Legendary = false, Godly = false, Mythic = false, Etherium = false, Celestial = false },
}

local rarityAliases = {
    Mythical = "Mythic",
    Ethereal = "Etherium",
    Ethereum = "Etherium",
}

local merchantItems = {
    "Easter2024Crate",
    "BasicPerkCrate",
    "HatDropBoost",
    "ComumEventPotion",
    "RareEventPotion",
    "EpicEventPotion",
    "LegendaryEventPotion",
    "GodlyEventPotion",
    "MythicEventPotion",
    "MoleBoost",
    "EventBooster",
}

local blur = Instance.new("BlurEffect")
blur.Size = 0
blur.Parent = Lighting

local Window = Rayfield:CreateWindow({
    Name = I.gift .. " Unboxing Hub V1",
    LoadingTitle = I.box .. " Unboxing Hub",
    LoadingSubtitle = "by DrSkrr",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "UnboxingHub",
        FileName = "Config",
    },
    Discord = {
        Enabled = true,
        Invite = "myjsXgDEC3",
        RememberJoins = true,
    },
    KeySystem = false,
})

local FarmTab = Window:CreateTab(I.box .. " Farm", 4483362458)
local EventTab = Window:CreateTab(I.event .. " Event", 4483362458)
local RewardsTab = Window:CreateTab(I.gift .. " Rewards", 4483362458)
local CratesTab = Window:CreateTab(I.box .. " Crates", 4483362458)
local AutoSellTab = Window:CreateTab(I.sell .. " Auto Sell", 4483362458)
local AutoAttackTab = Window:CreateTab(I.attack .. " Auto Attack", 4483362458)
local AreaFarmTab = Window:CreateTab(I.map .. " Area Farm", 4483362458)
local WheelTab = Window:CreateTab(I.wheel .. " Wheel", 4483362458)
local QuestTab = Window:CreateTab(I.quest .. " Quest", 4483362458)
local WebhookTab = Window:CreateTab(I.bell .. " Webhook", 4483362458)
local StatsTab = Window:CreateTab(I.stats .. " Stats", 4483362458)
local VisualTab = Window:CreateTab(I.eye .. " Visual", 4483362458)
local OtherTab = Window:CreateTab(I.gear .. " Other", 4483362458)

local function notify(title, content, duration)
    pcall(function()
        Rayfield:Notify({
            Title = tostring(title),
            Content = tostring(content),
            Duration = duration or 3,
            Image = 4483362458,
        })
    end)
end

local function formatTime(seconds)
    seconds = math.floor(seconds)
    local h = math.floor(seconds / 3600)
    local m = math.floor((seconds % 3600) / 60)
    local s = seconds % 60
    return string.format("%02d:%02d:%02d", h, m, s)
end

local function countDict(tbl)
    local count = 0
    for _ in pairs(tbl or {}) do
        count = count + 1
    end
    return count
end

local function getItemData(invItem)
    if not invItem or not invItem.ItemId then return nil end
    local ok, data = pcall(function()
        return ItemData.Get(invItem.ItemId)
    end)
    if ok then return data end
    return nil
end

local function getRarityName(itemData)
    if not itemData or not itemData.Rarity then return nil end
    local ok, rarityName = pcall(function()
        return ItemData.GetRawRarityName(itemData.Rarity)
    end)
    if ok then return rarityAliases[rarityName] or rarityName end
    return nil
end

local function getBestPerkStat(invItem)
    local best = 0

    local allowedKeys = {
        stat = true,
        stats = true,
        boost = true,
        boosts = true,
        multiplier = true,
        multipliers = true,
        amount = true,
        value = true,
        values = true,
        percent = true,
        percentage = true,
        modifier = true,
        modifiers = true,
    }

    local blockedKeys = {
        id = true,
        uid = true,
        uniqueid = true,
        itemid = true,
        level = true,
        xp = true,
        exp = true,
        time = true,
        timestamp = true,
        created = true,
        equipped = true,
        slot = true,
        price = true,
        cost = true,
        seed = true,
    }

    local function keyAllowed(key)
        key = string.lower(tostring(key or ""))

        for blocked in pairs(blockedKeys) do
            if key:find(blocked) then return false end
        end

        for allowed in pairs(allowedKeys) do
            if key:find(allowed) then return true end
        end

        return false
    end

    local function scan(value, key)
        if type(value) == "number" then
            if keyAllowed(key) and value >= perkFavoriteMinStat and value <= 100 then
                if value > best then best = value end
            end
        elseif type(value) == "table" then
            for k, v in pairs(value) do
                scan(v, k)
            end
        end
    end

    scan(invItem, "root")

    local data = getItemData(invItem)
    if data then scan(data, "itemData") end

    return best
end

local function isRareDrop(invItem)
    if not invItem then return false end
    local txt = string.lower(tostring(invItem.ItemId or invItem.Id or invItem.Name or ""))

    return txt:find("eggrain")
        or txt:find("egg rain")
        or txt:find("bombrain")
        or txt:find("bomb rain")
        or txt:find("bomb%-rain")
end

local function favoriteOrLockItem(category, uid)
    pcall(function()
        if RE:FindFirstChild("FavoriteItem") then
            RE.FavoriteItem:FireServer(category, uid, true)
        elseif RE:FindFirstChild("LockItem") then
            RE.LockItem:FireServer(category, uid, true)
        elseif RE:FindFirstChild("ToggleFavorite") then
            RE.ToggleFavorite:FireServer(category, uid)
        elseif RF:FindFirstChild("FavoriteItem") then
            RF.FavoriteItem:InvokeServer(category, uid, true)
        elseif RF:FindFirstChild("LockItem") then
            RF.LockItem:InvokeServer(category, uid, true)
        elseif RF:FindFirstChild("ToggleFavorite") then
            RF.ToggleFavorite:InvokeServer(category, uid)
        end
    end)
end

local function shouldSellItem(invItem, category)
    if not invItem then return false end
    if invItem.IsEquipped or invItem.IsFavorite or invItem.Locked then return false end

    local data = getItemData(invItem)
    local rarity = getRarityName(data)

    return rarity and sellRarities[category] and sellRarities[category][rarity] == true
end

local function protectSpecialItems()
    local data = PlayerData.data
    if not data then return end

    for _, category in ipairs({ "Hats", "Pets", "Perks" }) do
        local storage = data[category]

        if storage then
            for uid, invItem in pairs(storage) do
                if autoFavoriteRareDrops and isRareDrop(invItem) then
                    favoriteOrLockItem(category, uid)
                    invItem.IsFavorite = true
                    invItem.Locked = true
                elseif category == "Perks" and autoFavoriteGoodPerks and getBestPerkStat(invItem) >= perkFavoriteMinStat then
                    favoriteOrLockItem(category, uid)
                    invItem.IsFavorite = true
                    invItem.Locked = true
                end
            end
        end
    end
end

local function sellInventoryCategory(category)
    local data = PlayerData.data
    if not data then return end

    local storage = data[category]
    if not storage then return end

    local list = {}

    for uid, invItem in pairs(storage) do
        local protected = false

        if autoFavoriteRareDrops and isRareDrop(invItem) then
            favoriteOrLockItem(category, uid)
            invItem.IsFavorite = true
            invItem.Locked = true
            protected = true
        end

        if category == "Perks" and autoFavoriteGoodPerks and getBestPerkStat(invItem) >= perkFavoriteMinStat then
            favoriteOrLockItem(category, uid)
            invItem.IsFavorite = true
            invItem.Locked = true
            protected = true
        end

        if not protected and shouldSellItem(invItem, category) then
            table.insert(list, uid)
        end
    end

    if (category == "Hats" or category == "Pets") and #list >= countDict(storage) then
        table.remove(list)
    end

    if #list <= 0 then return end

    local ok, result = pcall(function()
        return RF.SellItems:InvokeServer(list, category)
    end)

    if ok and result then
        Stats.itemsSold = Stats.itemsSold + #list
        if category == "Hats" then Stats.hatsSold = Stats.hatsSold + #list end
        if category == "Pets" then Stats.petsSold = Stats.petsSold + #list end
        if category == "Perks" then Stats.perksSold = Stats.perksSold + #list end
        notify(I.check .. " Auto Sell", "Vendeu " .. tostring(#list) .. " " .. category, 2)
    end
end

local function getNearestBox()
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return nil end

    local field = BoxController.GetCurrentField(root.Position)
    if not field then return nil end

    local nearest
    local nearestDist = autoAttackRange
    local boxes = field.VisibleBoxes or field.Boxes or {}

    for _, box in pairs(boxes) do
        if box and box.CFrame and box.Health and box.Damage then
            if box.Health > box.Damage and not box.IsOpen then
                local dist = (box.CFrame.Position - root.Position).Magnitude

                if dist < nearestDist then
                    nearest = box
                    nearestDist = dist
                end
            end
        end
    end

    return nearest
end

local function getCameraBoxTarget()
    local camera = workspace.CurrentCamera
    local viewport = camera.ViewportSize
    local center = Vector2.new(viewport.X / 2, viewport.Y / 2)

    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    local field = root and BoxController.GetCurrentField(root.Position)
    if not field then return nil end

    local bestBox
    local bestScore = 260

    for _, box in pairs(field.VisibleBoxes or field.Boxes or {}) do
        if box and box.CFrame and box.Health and box.Damage then
            if box.Health > box.Damage and not box.IsOpen then
                local screenPos, onScreen = camera:WorldToViewportPoint(box.CFrame.Position)

                if onScreen and screenPos.Z > 0 then
                    local score = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude

                    if score < bestScore then
                        bestScore = score
                        bestBox = box
                    end
                end
            end
        end
    end

    return bestBox
end

local function buildRoutePoints(center, radius, step)
    local points = {}

    for x = -radius, radius, step do
        local row = {}

        for z = -radius, radius, step do
            if Vector3.new(x, 0, z).Magnitude <= radius then
                table.insert(row, center + Vector3.new(x, 0, z))
            end
        end

        if math.floor((x + radius) / step) % 2 == 1 then
            local rev = {}
            for i = #row, 1, -1 do
                table.insert(rev, row[i])
            end
            row = rev
        end

        for _, point in ipairs(row) do
            table.insert(points, point)
        end
    end

    return points
end

local function getAreaModel(index)
    local areas = workspace:FindFirstChild("Areas")
    if not areas then return nil end

    index = tonumber(index)
    if not index then return nil end

    local function areaMatches(inst)
        local name = tostring(inst.Name)
        local numberFromStart = tonumber(name:match("^(%d+)[_%-%s]"))
        local numberFromArea = tonumber(name:match("[Aa]rea[%s_%-]*(%d+)"))

        if numberFromStart == index or numberFromArea == index then
            return true
        end

        for _, attr in ipairs({ "Id", "ID", "AreaId", "AreaID", "World", "WorldId", "WorldID", "Index" }) do
            local ok, value = pcall(function()
                return inst:GetAttribute(attr)
            end)

            if ok and tonumber(value) == index then
                return true
            end
        end

        return false
    end

    for _, inst in ipairs(areas:GetChildren()) do
        if areaMatches(inst) then return inst end
    end

    for _, inst in ipairs(areas:GetDescendants()) do
        if inst:IsA("Model") or inst:IsA("Folder") then
            if areaMatches(inst) then return inst end
        end
    end

    return nil
end

local function getModelCenter(model)
    if not model then return nil end

    for _, name in ipairs({ "Center", "Spawn", "Teleport", "Hitbox", "Field", "Zone", "Base" }) do
        local inst = model:FindFirstChild(name, true)

        if inst and inst:IsA("BasePart") then
            return inst.Position
        elseif inst and inst:IsA("Model") then
            local ok, cf = pcall(function()
                return inst:GetPivot()
            end)
            if ok and cf then return cf.Position end
        end
    end

    local ok, cf = pcall(function()
        return model:GetPivot()
    end)

    if ok and cf then return cf.Position end

    local part = model:FindFirstChildWhichIsA("BasePart", true)
    return part and part.Position or nil
end

local function startTPCameraRoute()
    if tpConn then tpConn:Disconnect() tpConn = nil end

    local char = player.Character or player.CharacterAdded:Wait()
    local root = char:WaitForChild("HumanoidRootPart")

    lockedCameraCenter = lockedCameraCenter or root.Position
    routePoints = buildRoutePoints(lockedCameraCenter, routeRadius, routeStep)
    routeIndex = 1

    tpConn = RunService.Heartbeat:Connect(function()
        if not State.tpCamera then return end

        local c = player.Character
        local r = c and c:FindFirstChild("HumanoidRootPart")
        local h = c and c:FindFirstChildOfClass("Humanoid")
        if not r or not h or #routePoints == 0 then return end

        local target = routePoints[routeIndex]
        local flatR = Vector3.new(r.Position.X, lockedCameraCenter.Y, r.Position.Z)
        local flatT = Vector3.new(target.X, lockedCameraCenter.Y, target.Z)

        if (flatR - flatT).Magnitude <= 8 then
            routeIndex = routeIndex + 1
            if routeIndex > #routePoints then routeIndex = 1 end
            target = routePoints[routeIndex]
        end

        h:MoveTo(target)
    end)
end

local function stopTPCameraRoute()
    if tpConn then tpConn:Disconnect() tpConn = nil end
end

local function startFarmAreaRoute()
    if areaConn then areaConn:Disconnect() areaConn = nil end

    local area = getAreaModel(farmAreaIndex)
    farmAreaCenter = getModelCenter(area)

    if not farmAreaCenter then
        notify(I.x .. " Area Farm", "Area nao encontrada: " .. tostring(farmAreaIndex), 3)
        return
    end

    local char = player.Character or player.CharacterAdded:Wait()
    local root = char:WaitForChild("HumanoidRootPart")
    root.CFrame = CFrame.new(farmAreaCenter + Vector3.new(0, 4, 0))

    farmPoints = buildRoutePoints(farmAreaCenter, farmAreaRadius, farmAreaStep)
    farmPointIndex = 1
    State.autoAttackBoxes = true

    areaConn = RunService.Heartbeat:Connect(function()
        if not autoFarmArea then return end

        local c = player.Character
        local r = c and c:FindFirstChild("HumanoidRootPart")
        local h = c and c:FindFirstChildOfClass("Humanoid")
        if not r or not h or #farmPoints == 0 then return end

        local target = farmPoints[farmPointIndex]
        local flatR = Vector3.new(r.Position.X, farmAreaCenter.Y, r.Position.Z)
        local flatT = Vector3.new(target.X, farmAreaCenter.Y, target.Z)

        if (flatR - flatT).Magnitude <= 8 then
            farmPointIndex = farmPointIndex + 1
            if farmPointIndex > #farmPoints then farmPointIndex = 1 end
            target = farmPoints[farmPointIndex]
        end

        h:MoveTo(target)
    end)

    notify(I.check .. " Area Farm", "Farmando area " .. tostring(farmAreaIndex), 3)
end

local function stopFarmAreaRoute()
    if areaConn then areaConn:Disconnect() areaConn = nil end
end

local function getEquippedPerkIds()
    local data = PlayerData.data
    local ids = {}
    if not data then return ids end

    if type(data.PerksEquipped) == "table" then
        for _, id in pairs(data.PerksEquipped) do
            if id then table.insert(ids, id) end
        end
    end

    if type(data.EquippedPerks) == "table" then
        for _, id in pairs(data.EquippedPerks) do
            if id then table.insert(ids, id) end
        end
    end

    if type(data.Perks) == "table" then
        for uid, perk in pairs(data.Perks) do
            if perk and (perk.IsEquipped or perk.Equipped or perk.Slot or perk.TeamIndex) then
                table.insert(ids, uid)
            end
        end
    end

    return ids
end

local function forceActivateEquippedPerks()
    for _, id in ipairs(getEquippedPerkIds()) do
        pcall(function()
            RE.ActivatePerk:FireServer(id)
        end)
        task.wait(0.15)
    end
end

local function recordRush(name)
    Stats.rushesDone = Stats.rushesDone + 1
    Stats.rushesByName[name] = (Stats.rushesByName[name] or 0) + 1
end

local function trackCrateReward(value)
    if type(value) == "string" then
        local s = string.lower(value)

        if s:find("eggrain") or s:find("egg rain") then
            Stats.eggRainGained = Stats.eggRainGained + 1
        end

        if s:find("bombrain") or s:find("bomb rain") or s:find("bomb%-rain") then
            Stats.bombRainGained = Stats.bombRainGained + 1
        end

        if s:find("ticket") then
            Stats.ticketsGained = Stats.ticketsGained + 1
        end
    elseif type(value) == "table" then
        for k, v in pairs(value) do
            local key = string.lower(tostring(k))

            if key:find("ticket") and type(v) == "number" then
                Stats.ticketsGained = Stats.ticketsGained + v
            end

            trackCrateReward(k)
            trackCrateReward(v)
        end
    end
end

local function openCrateAndTrack(crateId, amount)
    amount = tonumber(amount) or 1

    local ok, result = pcall(function()
        return RF.OpenCrate:InvokeServer(crateId, amount)
    end)

    if ok then
        Stats.cratesOpened = Stats.cratesOpened + amount
        Stats.cratesByName[crateId] = (Stats.cratesByName[crateId] or 0) + amount
        trackCrateReward(result)
    end
end

local function getNearestWheelId()
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    local nearestId
    local nearestDist = math.huge

    for _, inst in ipairs(workspace:GetDescendants()) do
        local ok, wheelId = pcall(function()
            return inst:GetAttribute("WheelId")
        end)

        if ok and wheelId then
            local pos

            if inst:IsA("BasePart") then
                pos = inst.Position
            elseif inst:IsA("Model") then
                pos = inst:GetPivot().Position
            end

            if pos then
                local dist = root and (pos - root.Position).Magnitude or 0

                if dist < nearestDist then
                    nearestDist = dist
                    nearestId = wheelId
                end
            end
        end
    end

    return nearestId
end

local function spinSelectedWheel()
    local wheelId = selectedWheelId

    if wheelId == "Auto Detect" then
        wheelId = getNearestWheelId()
    end

    if not wheelId then return end

    pcall(function()
        RF.SpinTicketWheel:InvokeServer(wheelId)
    end)
end

local function claimAllQuestLike()
    pcall(function() if RE:FindFirstChild("ClaimDungeonQuest") then RE.ClaimDungeonQuest:FireServer() end end)
    pcall(function() if RE:FindFirstChild("ClaimAllAchievements") then RE.ClaimAllAchievements:FireServer() end end)
    pcall(function() if RE:FindFirstChild("ClaimLoginReward") then RE.ClaimLoginReward:FireServer() end end)

    for i = 1, 30 do
        pcall(function() if RE:FindFirstChild("ClaimPlaytimeReward") then RE.ClaimPlaytimeReward:FireServer(i) end end)
        pcall(function() if RE:FindFirstChild("ClaimBattlePassTask") then RE.ClaimBattlePassTask:FireServer(i) end end)
        pcall(function() if RE:FindFirstChild("ClaimQuest") then RE.ClaimQuest:FireServer(i) end end)
        pcall(function() if RE:FindFirstChild("ClaimTask") then RE.ClaimTask:FireServer(i) end end)
    end
end

local function getMerchantData()
    if not RF:FindFirstChild("GetMerchantData") then return nil end

    local tries = {
        function() return RF.GetMerchantData:InvokeServer("MoleMerchant") end,
        function() return RF.GetMerchantData:InvokeServer({ MerchantId = "MoleMerchant" }) end,
        function() return RF.GetMerchantData:InvokeServer() end,
    }

    for _, fn in ipairs(tries) do
        local ok, data = pcall(fn)
        if ok and type(data) == "table" then return data end
    end

    return nil
end

local function collectMerchantItems(data, found)
    found = found or {}

    if type(data) ~= "table" then return found end

    local itemId = data.ItemId or data.ItemID or data.Id or data.ID or data.Name

    if itemId then
        table.insert(found, data)
    end

    for _, v in pairs(data) do
        if type(v) == "table" then
            collectMerchantItems(v, found)
        end
    end

    return found
end

local function merchantItemAvailable(item)
    if type(item) ~= "table" then return false end

    if item.SoldOut == true then return false end
    if item.IsSoldOut == true then return false end
    if item.Purchased == true then return false end
    if item.Bought == true then return false end
    if item.CanBuy == false then return false end
    if item.Available == false then return false end

    local stock = item.Stock or item.Amount or item.Quantity or item.Remaining or item.Left

    if stock ~= nil and tonumber(stock) ~= nil and tonumber(stock) <= 0 then
        return false
    end

    return true
end

local function merchantSignature(item)
    local itemId = tostring(item.ItemId or item.ItemID or item.Id or item.ID or item.Name or "")
    local stock = tostring(item.Stock or item.Amount or item.Quantity or item.Remaining or item.Left or "")
    local price = tostring(item.Price or item.Cost or item.CostAmount or "")
    local reset = tostring(item.RefreshAt or item.ResetAt or item.RestockAt or item.RestockTime or "")

    return itemId .. "|" .. stock .. "|" .. price .. "|" .. reset
end

local function buyMerchantItem(itemId)
    local ok = pcall(function()
        RF.PurchaseMerchantItem:InvokeServer({
            ItemId = itemId,
            MerchantId = "MoleMerchant",
        })
    end)

    if ok then
        Stats.merchantBought = Stats.merchantBought + 1
    end
end

local function buyMerchantSelectedOnce()
    if not merchantAutoBuyEnabled then return end

    local data = getMerchantData()
    if not data then return end

    local foundItems = collectMerchantItems(data)

    for _, item in ipairs(foundItems) do
        local itemId = tostring(item.ItemId or item.ItemID or item.Id or item.ID or item.Name or "")

        if merchantSelectedItems[itemId] and merchantItemAvailable(item) then
            local sig = merchantSignature(item)

            if not merchantBoughtCache[sig] then
                merchantBoughtCache[sig] = true
                task.spawn(function()
                    buyMerchantItem(itemId)
                end)
            end
        end
    end
end

local function sendWebhook(message)
    if webhookUrl == "" then
        notify(I.bell .. " Webhook", "Coloque a URL primeiro.", 3)
        return
    end

    local req = http_request or request or syn and syn.request or fluxus and fluxus.request

    if not req then
        notify(I.x .. " Webhook", "Executor sem request/http_request.", 4)
        return
    end

    pcall(function()
        req({
            Url = webhookUrl,
            Method = "POST",
            Headers = { ["Content-Type"] = "application/json" },
            Body = HttpService:JSONEncode({ content = tostring(message) }),
        })
    end)
end

local function buildWebhookReport()
    local lines = {}

    table.insert(lines, "Unboxing Hub - Relatorio")
    table.insert(lines, "Player: " .. tostring(player.Name))
    table.insert(lines, "Tempo: " .. formatTime(os.clock() - Stats.startedAt))
    table.insert(lines, "Easter Rush: " .. tostring(Stats.rushesByName["Easter Rush"] or 0))
    table.insert(lines, "Gold Rush: " .. tostring(Stats.rushesByName["Gold Rush"] or 0))
    table.insert(lines, "Caixas quebradas: " .. tostring(Stats.boxesBroken))
    table.insert(lines, "Crates abertas: " .. tostring(Stats.cratesOpened))
    table.insert(lines, "EggRain: " .. tostring(Stats.eggRainGained))
    table.insert(lines, "BombRain: " .. tostring(Stats.bombRainGained))
    table.insert(lines, "Tickets: " .. tostring(Stats.ticketsGained))
    table.insert(lines, "Merchant comprados: " .. tostring(Stats.merchantBought))
    table.insert(lines, "Vendidos: " .. tostring(Stats.itemsSold))
    table.insert(lines, "Hats vendidos: " .. tostring(Stats.hatsSold))
    table.insert(lines, "Pets vendidos: " .. tostring(Stats.petsSold))
    table.insert(lines, "Perks vendidos: " .. tostring(Stats.perksSold))
    table.insert(lines, "Crates:")

    local hasCrate = false

    for crateName, count in pairs(Stats.cratesByName) do
        hasCrate = true
        table.insert(lines, "- " .. tostring(crateName) .. ": " .. tostring(count))
    end

    if not hasCrate then
        table.insert(lines, "- nenhuma")
    end

    return table.concat(lines, "\n")
end

pcall(function()
    RE.BoxesDestroyed.OnClientEvent:Connect(function(_, boxBuffer)
        local count = 0

        pcall(function()
            count = buffer.len(boxBuffer) // 2
        end)

        Stats.boxesBroken = Stats.boxesBroken + count
    end)
end)

FarmTab:CreateSection(I.money .. " Farm")

FarmTab:CreateInput({
    Name = I.box .. " Mundo do Auto Gold Rush",
    PlaceholderText = "Ex: 108",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n then autoRushWorld = n end
    end,
})

FarmTab:CreateToggle({ Name = I.money .. " Auto Gold Rush", CurrentValue = false, Flag = "AutoGoldRush", Callback = function(v) State.autoGoldRush = v end })
FarmTab:CreateToggle({ Name = I.egg .. " Auto Easter Rush", CurrentValue = false, Flag = "AutoEasterRush", Callback = function(v) State.autoEasterRush = v end })
FarmTab:CreateToggle({ Name = I.x .. " Auto Close", CurrentValue = false, Flag = "AutoClose", Callback = function(v) State.autoClose = v end })
FarmTab:CreateToggle({ Name = I.check .. " Auto Close Rush Results", CurrentValue = false, Flag = "AutoCloseRushResults", Callback = function(v) State.autoCloseRushResults = v end })
FarmTab:CreateToggle({ Name = I.shield .. " NeverGlitch", CurrentValue = false, Flag = "NeverGlitch", Callback = function(v) State.autoPerk = v end })

FarmTab:CreateButton({
    Name = I.bomb .. " BOMB RAIN",
    Callback = function()
        pcall(function()
            RE.PromptProductPurchase:FireServer("BombRainInstant")
        end)
    end,
})

EventTab:CreateSection(I.event .. " Mole Merchant Auto Buy")

EventTab:CreateToggle({
    Name = I.check .. " Ativar Auto Buy Merchant",
    CurrentValue = false,
    Flag = "MerchantAutoBuyEnabled",
    Callback = function(Value)
        merchantAutoBuyEnabled = Value
    end,
})

EventTab:CreateInput({
    Name = I.gear .. " Delay Scan Merchant",
    PlaceholderText = "Padrao: 0.25",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n >= 0.1 then merchantScanDelay = n end
    end,
})

for _, itemId in ipairs(merchantItems) do
    EventTab:CreateToggle({
        Name = I.gift .. " Comprar " .. itemId,
        CurrentValue = false,
        Flag = "Merchant_" .. itemId,
        Callback = function(Value)
            merchantSelectedItems[itemId] = Value
        end,
    })
end

EventTab:CreateButton({
    Name = I.money .. " Comprar Selecionados Agora",
    Callback = function()
        for _, itemId in ipairs(merchantItems) do
            if merchantSelectedItems[itemId] then
                task.spawn(function()
                    buyMerchantItem(itemId)
                end)
            end
        end
    end,
})

EventTab:CreateButton({
    Name = I.gear .. " Limpar Cache Merchant",
    Callback = function()
        merchantBoughtCache = {}
        notify(I.check .. " Merchant", "Cache limpo.", 2)
    end,
})

RewardsTab:CreateSection(I.gift .. " Rewards")
RewardsTab:CreateToggle({ Name = I.money .. " Auto Claim", CurrentValue = false, Flag = "AutoClaim", Callback = function(v) State.autoClaim = v end })
RewardsTab:CreateToggle({ Name = I.attack .. " Battle Pass", CurrentValue = false, Flag = "BattlePass", Callback = function(v) State.autoBP = v end })
RewardsTab:CreateToggle({ Name = I.gift .. " Daily Login", CurrentValue = false, Flag = "DailyLogin", Callback = function(v) State.autoDaily = v end })
RewardsTab:CreateToggle({ Name = I.star .. " Achievements", CurrentValue = false, Flag = "Achievements", Callback = function(v) State.autoAch = v end })

CratesTab:CreateSection(I.box .. " Crates")

CratesTab:CreateInput({
    Name = I.box .. " Quantidade",
    PlaceholderText = "Ex: 10",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n > 0 then crateAmount = n else crateAmount = 1 end
    end,
})

local function crateButton(name, crateId)
    CratesTab:CreateButton({
        Name = I.box .. " " .. name,
        Callback = function()
            openCrateAndTrack(crateId, crateAmount)
        end,
    })
end

crateButton("EasterCrate", "Easter2024Crate")
crateButton("MaterialCrate", "MaterialCrate")
crateButton("BasicPerk", "BasicPerkCrate")
crateButton("BoxPerk", "BoxPerkCrate")
crateButton("ExclusiveEasterCrate", "ExclusiveEasterCrate")
crateButton("Alien DungeonCrateWorld3", "DungeonCrateWorld3")
crateButton("CrystalCave DungeonCrateWorld1", "DungeonCrateWorld1")

AutoSellTab:CreateSection(I.shield .. " Protecao")
AutoSellTab:CreateToggle({ Name = I.bomb .. " Favoritar EggRain/BombRain", CurrentValue = true, Flag = "AutoFavoriteRareDrops", Callback = function(v) autoFavoriteRareDrops = v end })
AutoSellTab:CreateToggle({ Name = I.star .. " Favoritar Perks bons", CurrentValue = true, Flag = "AutoFavoriteGoodPerks", Callback = function(v) autoFavoriteGoodPerks = v end })

AutoSellTab:CreateInput({
    Name = I.star .. " Stat minimo do Perk",
    PlaceholderText = "Padrao: 4.0",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n then perkFavoriteMinStat = n end
    end,
})

AutoSellTab:CreateInput({
    Name = I.gear .. " Delay Auto Sell",
    PlaceholderText = "Padrao: 5",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n >= 1 then autoSellDelay = n end
    end,
})

local rarityOrder = { "Common", "Rare", "Epic", "Legendary", "Godly", "Mythic", "Etherium", "Celestial" }

local function rarityToggles(category, icon)
    AutoSellTab:CreateSection(icon .. " Sell " .. category)

    AutoSellTab:CreateToggle({
        Name = I.sell .. " Ativar Auto Sell " .. category,
        CurrentValue = false,
        Flag = "AutoSell" .. category,
        Callback = function(v)
            State["autoSell" .. category] = v
        end,
    })

    for _, rarity in ipairs(rarityOrder) do
        AutoSellTab:CreateToggle({
            Name = rarity,
            CurrentValue = sellRarities[category][rarity],
            Flag = "Sell" .. category .. rarity,
            Callback = function(v)
                sellRarities[category][rarity] = v
            end,
        })
    end
end

rarityToggles("Hats", I.hat)
rarityToggles("Pets", I.pet)
rarityToggles("Perks", I.star)

AutoSellTab:CreateButton({ Name = I.shield .. " Proteger Agora", Callback = function() protectSpecialItems() notify(I.check .. " Protecao", "Verificado.", 2) end })
AutoSellTab:CreateButton({ Name = I.sell .. " Vender Agora", Callback = function() protectSpecialItems() sellInventoryCategory("Hats") sellInventoryCategory("Pets") sellInventoryCategory("Perks") end })

AutoAttackTab:CreateSection(I.attack .. " Auto Attack")
AutoAttackTab:CreateToggle({ Name = I.attack .. " Auto Attack Aura", CurrentValue = false, Flag = "AutoAttackBoxes", Callback = function(v) State.autoAttackBoxes = v end })

AutoAttackTab:CreateInput({
    Name = I.attack .. " Distancia",
    PlaceholderText = "Padrao: 65",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n > 0 then autoAttackRange = n end
    end,
})

AutoAttackTab:CreateInput({
    Name = I.gear .. " Delay",
    PlaceholderText = "Padrao: 0.05",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n >= 0.01 then autoAttackDelay = n end
    end,
})

AutoAttackTab:CreateToggle({
    Name = I.eye .. " Auto Attack Camera Lock",
    CurrentValue = false,
    Flag = "AutoAttackCameraLock",
    Callback = function(v)
        State.autoAttackCameraLock = v
        local camera = workspace.CurrentCamera

        if v then
            local char = player.Character or player.CharacterAdded:Wait()
            local root = char:WaitForChild("HumanoidRootPart")

            lockedCameraCenter = lockedCameraCenter or root.Position
            root.CFrame = CFrame.new(lockedCameraCenter + Vector3.new(0, 3, 0))
            camera.CameraType = Enum.CameraType.Scriptable

            if camAttackConn then camAttackConn:Disconnect() end

            local angle = 0

            camAttackConn = RunService.RenderStepped:Connect(function(dt)
                if not State.autoAttackCameraLock then return end

                angle = angle + dt * 1.2

                local x = math.cos(angle) * 10
                local z = math.sin(angle) * 10

                camera.CFrame = CFrame.new(lockedCameraCenter + Vector3.new(x, 4, z), lockedCameraCenter)
            end)

            State.tpCamera = true
            startTPCameraRoute()
        else
            if camAttackConn then camAttackConn:Disconnect() camAttackConn = nil end
            camera.CameraType = Enum.CameraType.Custom
            State.tpCamera = false
            stopTPCameraRoute()
        end
    end,
})

AreaFarmTab:CreateSection(I.map .. " Area Farm")

AreaFarmTab:CreateInput({
    Name = I.map .. " Numero da Area",
    PlaceholderText = "Ex: 1",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n then farmAreaIndex = n end
    end,
})

AreaFarmTab:CreateInput({
    Name = I.map .. " Raio",
    PlaceholderText = "Padrao: 90",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n >= 20 then farmAreaRadius = n end
    end,
})

AreaFarmTab:CreateInput({
    Name = I.map .. " Espacamento",
    PlaceholderText = "Padrao: 28",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n >= 8 then farmAreaStep = n end
    end,
})

AreaFarmTab:CreateToggle({ Name = I.attack .. " Auto Farm Area + Attack", CurrentValue = false, Flag = "AutoFarmArea", Callback = function(v) autoFarmArea = v if v then startFarmAreaRoute() else stopFarmAreaRoute() end end })

AreaFarmTab:CreateButton({
    Name = I.map .. " Ir para Area",
    Callback = function()
        local center = getModelCenter(getAreaModel(farmAreaIndex))

        if center then
            local char = player.Character or player.CharacterAdded:Wait()
            local root = char:WaitForChild("HumanoidRootPart")
            root.CFrame = CFrame.new(center + Vector3.new(0, 4, 0))
        else
            notify(I.x .. " Area Farm", "Area nao encontrada.", 2)
        end
    end,
})

WheelTab:CreateSection(I.wheel .. " Wheel")

WheelTab:CreateDropdown({
    Name = I.wheel .. " Wheel",
    Options = { "Auto Detect", "Easter", "Default" },
    CurrentOption = { "Auto Detect" },
    MultipleOptions = false,
    Flag = "SelectedWheel",
    Callback = function(opt)
        selectedWheelId = opt[1]
    end,
})

WheelTab:CreateInput({ Name = I.wheel .. " WheelId Manual", PlaceholderText = "Opcional", RemoveTextAfterFocusLost = false, Callback = function(Text) if Text and Text ~= "" then selectedWheelId = Text end end })
WheelTab:CreateInput({ Name = I.gear .. " Delay Wheel", PlaceholderText = "Padrao: 0.03", RemoveTextAfterFocusLost = false, Callback = function(Text) local n = tonumber(Text) if n and n >= 0.01 then wheelDelay = n end end })
WheelTab:CreateInput({ Name = I.wheel .. " Burst", PlaceholderText = "Padrao: 1", RemoveTextAfterFocusLost = false, Callback = function(Text) local n = tonumber(Text) if n and n >= 1 then wheelBurst = math.floor(n) end end })
WheelTab:CreateToggle({ Name = I.wheel .. " Auto Wheel", CurrentValue = false, Flag = "AutoWheel", Callback = function(v) State.autoWheel = v end })
WheelTab:CreateButton({ Name = I.wheel .. " Girar Agora", Callback = spinSelectedWheel })

QuestTab:CreateSection(I.quest .. " Quest")
QuestTab:CreateInput({ Name = I.gear .. " Delay Claim", PlaceholderText = "Padrao: 0.5", RemoveTextAfterFocusLost = false, Callback = function(Text) local n = tonumber(Text) if n and n >= 0.1 then questClaimDelay = n end end })
QuestTab:CreateToggle({ Name = I.quest .. " Auto Claim Quest", CurrentValue = false, Flag = "AutoQuestClaim", Callback = function(v) State.autoQuestClaim = v end })
QuestTab:CreateButton({ Name = I.quest .. " Claim Agora", Callback = claimAllQuestLike })

WebhookTab:CreateSection(I.bell .. " Discord Webhook")

WebhookTab:CreateInput({
    Name = I.bell .. " Webhook URL",
    PlaceholderText = "Cole o webhook aqui",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        webhookUrl = tostring(Text or "")
    end,
})

WebhookTab:CreateInput({
    Name = I.gear .. " Tempo em minutos",
    PlaceholderText = "Padrao: 60",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local n = tonumber(Text)
        if n and n >= 1 then webhookDelayMinutes = n end
    end,
})

WebhookTab:CreateToggle({ Name = I.bell .. " Ativar Webhook Automatico", CurrentValue = false, Flag = "WebhookEnabled", Callback = function(v) State.webhookEnabled = v lastWebhookAt = os.clock() end })
WebhookTab:CreateButton({ Name = I.check .. " Test Webhook", Callback = function() sendWebhook("Teste do Unboxing Hub - Player: " .. tostring(player.Name)) notify(I.bell .. " Webhook", "Teste enviado.", 2) end })
WebhookTab:CreateButton({ Name = I.stats .. " Enviar Relatorio Agora", Callback = function() sendWebhook(buildWebhookReport()) notify(I.bell .. " Webhook", "Relatorio enviado.", 2) end })

StatsTab:CreateSection(I.stats .. " Stats")

StatsTab:CreateButton({
    Name = I.stats .. " Mostrar Stats",
    Callback = function()
        notify(
            I.stats .. " Sessao",
            "Tempo: " .. formatTime(os.clock() - Stats.startedAt)
                .. " | Boxes: " .. tostring(Stats.boxesBroken)
                .. " | Crates: " .. tostring(Stats.cratesOpened)
                .. " | Merchant: " .. tostring(Stats.merchantBought)
                .. " | Vendidos: " .. tostring(Stats.itemsSold),
            7
        )
    end,
})

VisualTab:CreateSection(I.eye .. " Visual")

VisualTab:CreateToggle({
    Name = I.eye .. " Camera Lock",
    CurrentValue = false,
    Flag = "CameraLock",
    Callback = function(v)
        State.camLock = v
        local camera = workspace.CurrentCamera

        if v then
            local char = player.Character or player.CharacterAdded:Wait()
            local root = char:WaitForChild("HumanoidRootPart")
            local center = root.Position

            lockedCameraCenter = center
            camera.CameraType = Enum.CameraType.Scriptable

            if camConn then camConn:Disconnect() end

            local angle = 0

            camConn = RunService.RenderStepped:Connect(function(dt)
                if not State.camLock then return end

                angle = angle + dt * 1.5

                local x = math.cos(angle) * 10
                local z = math.sin(angle) * 10

                camera.CFrame = CFrame.new(center + Vector3.new(x, 4, z), center)
            end)
        else
            if camConn then camConn:Disconnect() camConn = nil end
            camera.CameraType = Enum.CameraType.Custom
        end
    end,
})

VisualTab:CreateToggle({ Name = I.map .. " TPCamera Area Walk", CurrentValue = false, Flag = "TPCamera", Callback = function(v) State.tpCamera = v if v then startTPCameraRoute() else stopTPCameraRoute() end end })

OtherTab:CreateSection(I.gear .. " Other")

OtherTab:CreateButton({
    Name = I.eye .. " TurtleSpy",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Turtle-Brand/Turtle-Spy/main/source.lua", true))()
        end)
    end,
})

OtherTab:CreateButton({
    Name = I.shield .. " Anti Afk",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/hassanxzayn-lua/Anti-afk/main/antiafkbyhassanxzyn"))()
        end)
    end,
})

OtherTab:CreateButton({
    Name = I.check .. " Copy Discord",
    Callback = function()
        setclipboard("https://discord.gg/myjsXgDEC3")
        notify(I.check .. " Discord", "Copiado.", 2)
    end,
})

task.spawn(function()
    while task.wait(autoAttackDelay) do
        if State.autoAttackBoxes or State.autoAttackCameraLock then
            pcall(function()
                local box = State.autoAttackCameraLock and getCameraBoxTarget() or getNearestBox()

                if box and box.Field and box.Field.Id and box.Id then
                    RE.AttackBox:FireServer(box.Field.Id, box.Id, Clock.now())
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(autoSellDelay) do
        protectSpecialItems()

        if State.autoSellHats then pcall(function() sellInventoryCategory("Hats") end) end
        if State.autoSellPets then pcall(function() sellInventoryCategory("Pets") end) end
        if State.autoSellPerks then pcall(function() sellInventoryCategory("Perks") end) end
    end
end)

task.spawn(function()
    while task.wait(0.03) do
        if State.autoWheel and os.clock() - lastWheelTick >= wheelDelay then
            lastWheelTick = os.clock()

            for _ = 1, wheelBurst do
                task.spawn(spinSelectedWheel)
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        if State.autoQuestClaim and os.clock() - lastQuestClaimTick >= questClaimDelay then
            lastQuestClaimTick = os.clock()
            claimAllQuestLike()
        end
    end
end)

task.spawn(function()
    while task.wait(0.05) do
        if merchantAutoBuyEnabled and os.clock() - merchantLastScan >= merchantScanDelay then
            merchantLastScan = os.clock()
            buyMerchantSelectedOnce()
        end
    end
end)

task.spawn(function()
    while task.wait(5) do
        if State.webhookEnabled then
            if os.clock() - lastWebhookAt >= webhookDelayMinutes * 60 then
                lastWebhookAt = os.clock()
                sendWebhook(buildWebhookReport())
            end
        end
    end
end)

task.spawn(function()
    while task.wait(3) do
        if State.autoClose then
            pcall(function()
                StateUtil.ClosePage()
            end)
        end

        if State.autoCloseRushResults then
            pcall(function()
                if RE:FindFirstChild("ClaimBoxRush") then
                    RE.ClaimBoxRush:FireServer()
                end

                StateUtil.ClosePage("BoxRushResults")
            end)
        end

        if State.autoEasterRush then
            pcall(function()
                local box = workspace:FindFirstChild("Easter")

                if box and box:FindFirstChild("EasterBoxRush") then
                    recordRush("Easter Rush")
                    RE.StartBoxRush:FireServer(box.EasterBoxRush)
                end
            end)
        end

        if State.autoGoldRush then
            pcall(function()
                local areas = workspace:FindFirstChild("Areas")
                local goldMine = areas and areas:FindFirstChild("23_GoldMine")

                if goldMine and goldMine:FindFirstChild("GoldRush") then
                    recordRush("Gold Rush")
                    RE.StartBoxRush:FireServer(goldMine.GoldRush, autoRushWorld)
                end
            end)
        end

        if State.autoPerk then pcall(forceActivateEquippedPerks) end

        if State.autoClaim then
            for i = 1, 12 do
                task.wait(0.1)
                pcall(function() RE.ClaimPlaytimeReward:FireServer(i) end)
            end
        end

        if State.autoBP then
            for i = 1, 6 do
                task.wait(0.2)
                pcall(function() RE.ClaimBattlePassTask:FireServer(i) end)
            end
        end

        if State.autoDaily then pcall(function() RE.ClaimLoginReward:FireServer() end) end
        if State.autoAch then pcall(function() RE.ClaimAllAchievements:FireServer() end) end
    end
end)

notify(I.gift .. " Unboxing Hub", "GUI carregado com emojis, Merchant corrigido e Auto Attack antigo.", 4)
